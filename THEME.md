# PyDoc 主题系统

## 实现概述

已成功实现多主题切换功能，默认使用护眼绿色主题。

## 主题列表

### 1. 森林主题 (默认)
- **主题ID**: `forest`
- **设计理念**: 深林绿色基调，模拟林下学习环境
- **色彩方案**:
  - 背景: 深墨绿 (#0a1410, #142820)
  - 强调色: 春叶绿 (#7cb342, #aed581)
  - 高亮: 阳光黄 (#cddc39)
  - 暖色: 琥珀色 (#ffb74d)

### 2. 海洋主题
- **主题ID**: `ocean`
- **设计理念**: 深海蓝色调，柔和不刺眼
- **色彩方案**:
  - 背景: 深海蓝 (#0a1420, #0f1f35)
  - 强调色: 天空蓝 (#4fc3f7, #81d4fa)
  - 高亮: 亮蓝 (#29b6f6)
  - 暖色: 橙色 (#ffa726)

### 3. 晚霞主题
- **主题ID**: `sunset`
- **设计理念**: 温暖琥珀色，适合夜间阅读
- **色彩方案**:
  - 背景: 深棕褐 (#1a1410, #251f18)
  - 强调色: 珊瑚橙 (#ff8a65, #ffab91)
  - 高亮: 琥珀 (#ffa726)
  - 暖色: 浅橙 (#ffcc80)

### 4. 高对比主题
- **主题ID**: `contrast`
- **设计理念**: 无障碍访问，纯黑背景+鲜艳色彩
- **色彩方案**:
  - 背景: 纯黑 (#000000)
  - 强调色: 荧光绿 (#00ff00)
  - 次要色: 青色 (#00ffff)
  - 高亮: 黄色 (#ffff00)

## 技术实现

### 状态管理
- 使用 zustand 管理主题状态
- 通过 persist 中间件持久化到 localStorage
- 文件: `src/stores/theme.ts`

### CSS 变量系统
- 所有主题通过 CSS 自定义属性定义
- 使用 `data-theme` 属性切换主题
- 文件: `src/app/globals.css`

### 核心变量
```css
--bg-primary          /* 主背景 */
--bg-secondary        /* 次级背景 */
--bg-card             /* 卡片背景 */
--bg-elevated         /* 提升元素背景 */

--accent-primary      /* 主强调色 */
--accent-secondary    /* 次强调色 */
--accent-highlight    /* 高亮色 */
--accent-warm         /* 暖色调 */

--text-primary        /* 主文字 */
--text-secondary      /* 次级文字 */
--text-muted          /* 弱化文字 */

--glow-primary        /* 主发光效果 */
--glow-focus          /* 焦点发光 */

--code-bg             /* 代码背景 */
--code-highlight      /* 代码高亮 */
--code-border         /* 代码边框 */
```

### 组件

#### ThemePicker
- 位置: `src/components/theme/ThemePicker.tsx`
- 功能: 视觉化主题选择器，显示颜色预览圆点
- 特性: 下拉面板、动画过渡、当前主题指示

#### ThemeInitializer
- 位置: `src/components/theme/ThemeInitializer.tsx`
- 功能: 客户端主题初始化，同步主题状态到 DOM

### 防闪烁机制
在 `layout.tsx` 中添加内联脚本，在页面加载前从 localStorage 读取主题并应用，避免主题切换闪烁。

## 使用方式

### 首页
- 主题选择器固定在右上角
- 点击即可展开主题选项

### 学习页面
- 主题选择器集成在顶部导航栏右侧
- 所有可视化组件自动响应主题变化

## 特色设计

1. **呼吸式高亮**: 活动代码行使用柔和发光效果，而非硬边框
2. **渐变过渡**: 所有颜色变化均有 0.3s 过渡动画
3. **主题一致性**: 从背景到按钮到滚动条，全部使用主题变量
4. **视觉预览**: 主题选择器显示实际颜色圆点，而非文字描述

## 扩展新主题

在 `src/app/globals.css` 中添加新的 `[data-theme="新主题ID"]` 选择器，定义所有必需的 CSS 变量即可。
